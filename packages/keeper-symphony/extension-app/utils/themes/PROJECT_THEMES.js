import { THEMES } from 'symphony-bdk-ui-toolkit';

export const PROJECT_THEMES = [
  {
    mode: THEMES[0].mode,
    colors: {
      ...THEMES[0].colors,
    },
  },
  {
    mode: THEMES[1].mode,
    colors: {
      ...THEMES[1].colors,
    },
  },
];
